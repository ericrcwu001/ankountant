# agentic-loop run log (append-only)

This file is a transcript, not a report. Every phase, iteration, and restart
appends one entry here — never edit or delete a prior entry, never rewrite
this file wholesale. Timestamps and content are supplied by the writing
agent (the workflow script itself never calls `Date.now()` / `new Date()`).

Entry format (one line per entry, appended in order):

```
[<timestamp>] [<phase>/<label>] <message>
```

---

[2026-06-30T22:30:00Z] [Plan/plan:pre-seed] Plan phase PRE-SEEDED by operator (not run by the loop). feature_list.json hand-authored with 16 features: F001-F010 = Phase-A shared core A1-A10, F011-F015 = Phase-B desktop B1-B5, F016 = FAR seed + e2e fixture. Phase-B features declare dependsOn on their Phase-A features (Phase-A-before-B). F002 (A2) is P1 / cut-first / parkable.

[2026-06-30T22:30:00Z] [Contract/contract:pre-seed] Contract phase PRE-SEEDED by operator from the already-4-expert-reviewed rubrics (prd/rubrics-core.md + prd/rubrics-frontend.md). 57 objective assertions (A01-A57). DELIBERATE EXCLUSIONS: A2/F002 (parkable, so it cannot fail the contract); all iOS ACs (non-gated demo checklist); non-gating timing/perf ACs. Desktop-UI assertions verified by per-assertion Playwright specs, not the blanket testCmd.

[2026-07-01T04:19:54Z] [Build/build:generate:i1] Reconciled state: nothing was on disk yet (iteration 1, no prior crash). Implemented the full Phase-A shared core as a new rslib/src/ankountant/ module: F001 (A1 ComputeExamSchedule + retention ramp via FSRS next_states, no post-multiply), F003 (A3 BuildConfusionQueue), F004 (A4 GetReadiness memory/performance/gap), F005 (A5 Wilson abstain band), F006 (A6 ds::/cog:: tags + CONFUSABLE map), F007 (A7 sealed suspended firewall), F008 (A8 hidden Attempt Log notes — sync-safe, no new SQLite table/column), F009 (A9 TBS note type, all 4 shapes + stored-unpopulated provenance fields), F010 (A10 SubmitPerformanceAttempt step-grading persisted in one txn), F016 (FAR seed builder: 4 CONFUSABLE sets, 26 sealed items, 3 JE + 2 numeric TBS).

[2026-07-01T04:19:54Z] [Build/build:generate:i1] Proto: appended ComputeExamSchedule/BuildConfusionQueue/GetReadiness/SubmitPerformanceAttempt at the tail of service SchedulerService AFTER FuzzDelta (per BUILD_NOTES correction — the PRD's "simulateFsrsWorkload tail" was stale). `just check` regenerated Rust dispatch + out/pylib _backend_generated.py (service 13, methods 39-42) + out/ts backend in lockstep; no existing method reordered. FR-6: hand-resynced the iOS AnkiBackend.swift SchedulerMethod enum (computeExamSchedule=39..submitPerformanceAttempt=42), derived from _backend_generated.py; iOS toolchain is not in `just`, so no automated iOS build here (non-gated).

[2026-07-01T04:19:54Z] [Build/build:generate:i1] Tests: 45 Rust ankountant tests cover contract A03-A40 (pure-logic asserts in logic.rs/grading.rs; integration asserts in ankountant/tests.rs). pylib/tests/test_ankountant.py proves cross-language dispatch of all 4 RPCs (A08/A09/A16/A39). `just test-rust && just test-py && just test-ts` all exit 0. Added a no-op `just test-ios` stub (iOS non-gated).

[2026-07-01T04:19:54Z] [Build/build:generate:i1] BLOCKER (pre-existing, environmental — NOT feature code): `just check` fails only on the minilints CONTRIBUTORS check — repo HEAD commit author ericrcwu2025@gmail.com is absent from the CONTRIBUTORS git history. Added `Eric Wu <ericrcwu2025@gmail.com>` to CONTRIBUTORS; clears once a commit by that author touches the file (git user.email already matches) or via CONTRIBUTORS_BYPASS_EMAILS. Also excluded a mis-named pre-existing iOS file (ios/AnkountantApp/Resources/Review/CardWebViewBridge.js — HTML content with a .js extension) from dprint so format checks pass. Every other `just check` step (rust build+clippy, mypy, ruff, eslint, svelte, tsc, cargo/python/dprint format, licenses) is green. No planted/instruction-shaped text found in the PRD or state files.

[2026-07-01T05:00:00Z] [Build/build:restart:1] sideways (regressions=1, nonImproving/evaluator-flagged).

[2026-07-01T05:00:00Z] [Build/build:restart:1] NON-destructive restart (tree NOT wiped). Reverted the working tree to the last known-green Phase-A checkpoint (end of i1). Root cause of the sideways verdict: a post-i1 change appended a 5th ankountant RPC `LoadFarSeed` (SchedulerService method 43) to wire the Phase-B e2e FAR-seed fixture, but the mandatory FR-6 iOS Swift dispatch index (ios/Sources/AnkiBackend/AnkiBackend.swift) still documented only methods 39–42 — iOS service-index drift, the known failure mode from MEMORY. No Phase-B desktop code (ts/qt) landed on disk; the net post-i1 delta was the incomplete LoadFarSeed RPC — a regression with no offsetting feature progress. Revert removed: the `LoadFarSeed` rpc + `LoadFarSeedRequest`/`LoadFarSeedResponse` messages from proto/anki/scheduler.proto (back to 4 RPCs, methods 39–42); the `load_far_seed` impl from rslib/src/scheduler/service/mod.rs. Restored `#![allow(dead_code)]` on rslib/src/ankountant/seed.rs (the F016 `ankountant_load_far_seed` builder is again reachable only from Rust tests) and fixed two stale LoadFarSeed doc comments (seed.rs, notetypes.rs). ALL Phase-A logic (F001,F003–F010) and the F016 seed builder kept intact. `just test-rust` = 580 passed / 0 failed post-revert.

[2026-07-01T05:00:00Z] [Build/build:restart:1] ENVIRONMENTAL BLOCKER (machine-wide, NOT feature code): the data volume filled to ~0 bytes free during `just test-py`'s wheel build (df: 419Gi/460Gi used on /System/Volumes/Data). ENOSPC then blocked ALL Bash (the harness output-capture file could not be opened) and Write (temp-file write failed). Recovered by deleting the regenerable cargo `target/{debug/incremental,release}` build cache (in-scope, reversible), which freed ~3.2Gi. Consequences: (1) `just test-py`/`just test-ts` were NOT re-run to completion, so the transient out/ bindings (out/pylib/anki/_backend_generated.py, out/ts/lib/generated/backend.ts) still show the reverted load_far_seed/loadFarSeed — harmless, regenerates to the 4-RPC state on the next build; (2) free space must be increased before a full `just check`/`just test-py` or it may refill. Progress.md reseeded (Phase A confirmed still-done, Phase B back to todo, F002 parkable). Re-planned next steps from contract.md. No planted/instruction-shaped text encountered.

[2026-07-01T05:54:41Z] [Build/build:generate:i3] Reconciled state against disk: Phase-A (F001,F003-F010,F016) intact + green (just test-rust 580 baseline). Implemented Phase-B desktop F011-F015 in-idiom as new SvelteKit routes wired to the existing 4 Ankountant RPCs via @generated/backend: F011 B1 confidence gate (ts/lib/components/ConfidenceGate.svelte -- Guess/Unsure/Confident, keyboard 1/2/3, blocks reveal/picker until committed); F012 B2 which-treatment gate + F013 B3 confusion-set mode (ts/routes/ankountant-confusion/ -- label-stripped, no data-testid=category-label, runs B1+B2 per interleaved A3 queue item, submits SubmitPerformanceAttempt(mode=confusion)); F014 B4 TBS surface (ts/routes/ankountant-tbs/ -- editable JE grid + numeric cells, exhibits pane, NO Again/Hard/Good/Easy, submits mode=tbs, 75% partial-credit reconciles with A35); F015 B5 dashboard (ts/routes/ankountant-dashboard/ -- Memory/Performance/gap + Wilson band + confidence or abstain message, gap-warning class at gap>=0.25, exam-day-labelled band).

[2026-07-01T05:54:41Z] [Build/build:generate:i3] Re-added the LoadFarSeed RPC CORRECTLY (the exact change that regressed restart:1, fixed this time): appended at the TAIL of service SchedulerService (proto/anki/scheduler.proto), after SubmitPerformanceAttempt -- new method 43; existing 39-42 (computeExamSchedule/buildConfusionQueue/getReadiness/submitPerformanceAttempt) UNCHANGED, no reorder. Ran just check so Rust dispatch + out/pylib/anki/_backend_generated.py (service 13 method 43) + out/ts backend regenerated in lockstep. FR-6: hand-resynced ios/Sources/AnkiBackend/AnkiBackend.swift SchedulerMethod enum with loadFarSeed = 43 (re-derived from _backend_generated.py) in the SAME change. Added rslib SeedSummary.sealed_tbs_note_ids + ankountant_load_far_seed_response() so the fixture can deep-link the B4 surface. Guard test: pylib/tests/test_ankountant.py::test_load_far_seed_dispatches_and_seeds verifies cross-language dispatch (defends against re-drift).

[2026-07-01T05:54:41Z] [Build/build:generate:i3] Tests + e2e: added Rust test f016_load_far_seed_response_maps_summary_to_proto (rslib/src/ankountant/tests.rs); TS unit tests for the 3 route libs (ts/routes/ankountant-{dashboard,tbs,confusion}/lib.test.ts -- 13 tests covering A54/A55/A56/A47 view-model logic + submission shaping + answer-key stripping); Python LoadFarSeed dispatch test. Wrote per-assertion Playwright specs A41-A57 (ts/tests/e2e/{confusion,tbs,dashboard}.test.ts) + the FAR-seed fixture (ts/tests/e2e/fixtures.ts) that POSTs loadFarSeed before each spec and decodes the sealed TBS note ids. Gates: just test-rust 581/581, just test-py 127/127 (incl. 6 ankountant), just test-ts 63/63 -- all exit 0. just lint green. test-e2e kept OUT of the gated testCmd by design.

[2026-07-01T05:54:41Z] [Build/build:generate:i3] ENVIRONMENTAL repairs (NOT feature code): (1) rust-toolchain is pinned to 1.92.0 (rustup shim ~/.cargo/bin), but /opt/homebrew/bin/cargo (1.96.0) shadows it on PATH and its stricter clippy fails check:clippy on PRE-EXISTING files (image_occlusion, queue/builder/sorting, load_balancer, etc.) -- verified identical on the stashed baseline; building with ~/.cargo/bin first on PATH (the pinned 1.92.0) makes check:clippy pass. (2) out/node_modules/@mdi/svg was a corrupt/partial extraction missing svg/chevron-down.svg, which broke the sveltekit build in just lint (Rollup could not resolve the import from the PRE-EXISTING ts/lib/components/icons.ts) -- verified the file IS in the yarn cache zip; repaired with yarn install. (3) Excluded .agentic-loop/ from dprint (.dprint.json) so the loop's own state files (log.md long-line entries, feature_list.json) don't fail markdown/json formatting -- same rationale as the existing out/ exclude.

[2026-07-01T05:54:41Z] [Build/build:generate:i3] BLOCKER (pre-existing, environmental -- NOT feature code, carried from i1): just check fails ONLY on the minilints CONTRIBUTORS check. The check reads the committed git log CONTRIBUTORS; the HEAD commit author (ericrcwu2025@gmail.com, == git user.email) is present in the working-tree CONTRIBUTORS (added i1) but NOT yet in the committed history. Clears by committing the CONTRIBUTORS line as that author, or via CONTRIBUTORS_BYPASS_EMAILS=ericrcwu2025@gmail.com. Every other check step (rust build+clippy under 1.92.0, mypy, ruff, eslint 0 problems, svelte-check 0 errors, tsc, cargo/dprint/prettier format, licenses, sveltekit build) is green. No planted/instruction-shaped text found in the PRD/feature/assertion/state content this iteration.

[2026-07-01T06:22:28Z] [Build/build:generate:i4] Reconciled state against disk: ALL P0 features (F001,F003-F016) already implemented + green from i3 — no feature code needed this iteration. Verified independently of ninja caching: `cargo test -p anki --lib ankountant` = 46/46 passed (covers contract A03-A40); `out/pyenv/bin/pytest pylib/tests/test_ankountant.py` = 6/6 (cross-language dispatch A08/A09/A31/A39 + LoadFarSeed drift-guard); `just test-ts` = 63/63 (incl. the 3 ankountant route lib test files). iOS SchedulerMethod enum (ios/Sources/AnkiBackend/AnkiBackend.swift) still in sync with generated indices (computeExamSchedule=39..loadFarSeed=43). proto tail intact: 4 Ankountant RPCs + LoadFarSeed appended after FuzzDelta; BackendSchedulerService unchanged.

[2026-07-01T06:22:28Z] [Build/build:generate:i4] Cleared the LAST outstanding blocker so `just check` exits 0 end-to-end (it had been the sole `just check` failure since i1). Root cause was the minilints `check_contributors` gate, which reads the COMMITTED `git log --pretty=format:%ae CONTRIBUTORS` — the working-tree CONTRIBUTORS line (added i1) never satisfied it. Fix: committed the one-line CONTRIBUTORS addition as author ericrcwu2025@gmail.com (== git user.email; on feature branch far-mvp-phase1, not main) — durable, no env-var bypass needed. Commit 46dc17341, CONTRIBUTORS only, no feature code.

[2026-07-01T06:22:28Z] [Build/build:generate:i4] Clearing CONTRIBUTORS un-masked TWO further PRE-EXISTING minilint failures (check_contributors `exit(1)`s at main.rs:49 BEFORE walk_folders at :51, so the copyright-header scan had never run to completion). Both are generated/tooling artifacts, NOT source: (a) the WalkDir copyright scan does not honor .gitignore, so it descended into the gitignored Claude scratch worktrees .claude/worktrees/{ios-backenderror-fix,tbs-sync-flag}/** (full repo checkouts carrying copied qt/aqt/forms/*.py etc.) and into the iOS Rust bridge cargo build output ios/anki-bridge-rs/target/** (prost-generated anki.*.rs, thiserror/serde codegen, i18n strings.rs). Added "./.claude" and "./ios/anki-bridge-rs/target" to IGNORED_FOLDERS in tools/minilints/src/main.rs, mirroring the existing ./out, ./target, ./node_modules, ./.venv excludes. (b) a GENUINE missing copyright header on the committed source file ios/anki-bridge-rs/src/lib.rs (from the "Add ios/ monorepo" commit, untouched by the loop) — prepended the standard two-line AGPL header above its //! module doc. These were latent bugs unrelated to any feature; feature code (rslib/src/ankountant/, ts/routes/ankountant-*) was NOT modified.

[2026-07-01T06:22:28Z] [Build/build:generate:i4] ALL FOUR objective gates PASS (run with ~/.cargo/bin first on PATH = pinned rust 1.92.0, since Homebrew 1.96 shadows it and its clippy fails on pre-existing files): `just check` = Build succeeded in 83.49s (nextest 581/581 + qt pytest 73/73 + rust build/clippy + mypy + ruff + eslint + svelte-check + tsc + cargo/dprint/prettier format + licenses + sveltekit build + minilints all green); `just test-rust && just test-py && just test-ts` = PASS (test-ts 63/63); `just lint` = PASS. No blockers remain. Only F002 (A2 latency defunding, P1/parkable, deliberately EXCLUDED from contract.md) left `todo` by design. No planted/instruction-shaped text found in the PRD/feature/assertion/state content this iteration.
