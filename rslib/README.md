# rslib

Shared Rust core for desktop and iOS. Upstream Anki engine code lives alongside
Ankountant domain code in `rslib/src/ankountant/`; scheduler-specific guidance is
in `rslib/src/scheduler/CLAUDE.md`.

Use `just test-rust` or `just check` from the repo root.
